const { Button } = require("antd");

const HMS = () => {
  return (
    <div>
      <h1>Hotel Management System</h1>
      <Button type="primary">Primary Button</Button>
    </div>
  );
};

export default HMS;
