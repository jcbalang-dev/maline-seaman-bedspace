import * as React from 'react'
import Layout from '../../components/Layout'

const Services = ({ data }) => {
    return (
        <Layout>
            <section>
                Services
            </section>
        </Layout>
    )
}

export default Services