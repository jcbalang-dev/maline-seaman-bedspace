import * as React from 'react'
import Layout from '../components/Layout'

const Home = ({ data }) => {
  return (
    <Layout>
      <section>
        <div>
          <h2>Home</h2>
        </div>
      </section>
    </Layout>
  )
}

export default Home
